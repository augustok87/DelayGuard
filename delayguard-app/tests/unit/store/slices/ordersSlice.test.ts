import ordersSlice from '../../../src/store/slices/ordersSlice';

describe('ordersSlice', () => {
  it('should be able to import the Redux slice', () => {
    expect(ordersSlice).toBeDefined();
  });
});